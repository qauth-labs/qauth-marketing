# Resend (https://resend.com) — powers the waitlist signup form.
# Create an API key and a Segment in the Resend dashboard, then set these
# as environment variables in Netlify (or a local .env for `bun run dev`).
RESEND_API_KEY=
RESEND_SEGMENT_ID=
